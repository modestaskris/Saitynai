// Auth
const LOGIN = "/login";
const REGISTER = "/register";
// others
const LANDING = "/";
const CATEGORIES = "/categories";
const PLAYLISTS = "/playlists";
const SONGS = "/songs";
const USERS = "/users";
const NOTFOUND = "/:pathMatch(.*)*";

export default {
  // Auth
  LOGIN,
  REGISTER,
  // others
  LANDING,
  CATEGORIES,
  PLAYLISTS,
  SONGS,
  USERS,
  NOTFOUND,
};
